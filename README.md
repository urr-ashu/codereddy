# CodeReddy

Static Next.js landing website for `codereddy.in`.

## Development

```bash
npm install
npm run dev
```

## Render Static Site

- Build command: `npm install && npm run build`
- Publish directory: `out`

## Before Launch

Update `whatsappNumber` in `app/page.tsx` with the real WhatsApp number.
